{
  "name": "gotiprep-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "website"
  ],
  "scripts": {
    "build": "cd website && npm run build",
    "dev": "cd website && npm run dev"
  }
}
